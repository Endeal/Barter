group 'com.endeal.barter'
version '1'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

model {
    components {
        barter(JvmLibrarySpec)
    }
}